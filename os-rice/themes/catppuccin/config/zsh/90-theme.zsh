# 90-theme.zsh — rice-owned prompt (catppuccin). LAYER: swapped on rice switch (§6).
export OSR_RICE_THEME="catppuccin"
export STARSHIP_CONFIG="$HOME/.config/starship.toml"
eval "$(starship init zsh)"
