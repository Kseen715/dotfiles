# 90-theme.zsh — rice-owned prompt (void-i3-rosemuted). LAYER: swapped on rice
# switch (§6). Untouched by a switch: 00-env, 10-omz, 20-aliases, 99-local.
export OSR_RICE_THEME="rosemuted"
export STARSHIP_CONFIG="$HOME/.config/starship.toml"
eval "$(starship init zsh)"
