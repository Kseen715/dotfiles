# 90-theme.zsh template — rice-owned prompt, rendered per theme (§6b).
# LAYER: swapped on theme switch.
export OSR_RICE_THEME="{{THEME}}"
export STARSHIP_CONFIG="$HOME/.config/starship.toml"
eval "$(starship init zsh)"
