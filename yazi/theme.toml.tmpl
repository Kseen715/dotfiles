# yazi theme template — the ONE yazi theme layer, rendered per theme (§6b).
# Not a palette: yazi themes are whole flavors, so the theme names one with
# `flavor:` in its theme.list and the flavor itself lives in dotfiles/yazi/flavors/.
[flavor]
dark = "{{yazi_flavor}}"
