# yazi theme template — rendered per theme (§6b). Selects the flavor the same
# theme renders from yazi/flavor.toml.tmpl, so the two can never disagree about
# a name: both are {{THEME}}.
[flavor]
dark = "{{THEME}}"
