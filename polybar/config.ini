;; polybar/config.ini — dotfiles-owned base layer (os-rice §5).
;; Geometry, fonts and which modules appear. Colors come from colors.ini, which
;; the rice owns and swaps on a rice switch — nothing in this file is a hex code.

[settings]
screenchange-reload = true
pseudo-transparency = false
compositing-background = source
compositing-foreground = over
compositing-border = over

[global/wm]
margin-bottom = 0
margin-top = 0
include-file = ~/.config/polybar/colors.ini
include-file = ~/.config/polybar/modules.ini

[bar/main]
monitor = ${env:MONITOR:}
width = 100%
height = 16px
radius = 0
bottom = false
fixed-center = true
wm-restack = i3
enable-ipc = true

background = ${color.background}
foreground = ${color.foreground}

line-size = 2px
border-size = 0pt
padding-left = 2
padding-right = 2
module-margin = 1

;; JetBrainsMono Nerd Font is installed by modules/theming.sh; the symbol-only
;; fallback covers glyphs a patched font can still miss, and Noto Color Emoji is
;; last so emoji in window titles do not render as tofu.
;;
;; Sized to the 16px bar. The trailing number is the vertical offset, and it is
;; what actually centers each font: they have different ascents, so one offset
;; for all three leaves the icon fonts sitting low. At this height there is ~2px
;; of slack in total, so a font that is one point too big is not clipped, it is
;; simply not drawn - keep these in step with `height` above.
font-0 = "JetBrainsMono Nerd Font:style=Medium:size=8;1"
font-1 = "Symbols Nerd Font:size=10;2"
font-2 = "Noto Color Emoji:scale=8;1"

modules-left = i3 xwindow
modules-center = date
modules-right = pulseaudio backlight cpu memory temperature wlan battery systray powermenu

cursor-click = pointer
cursor-scroll = ns-resize

[bar/secondary]
;; Every output after the first. Identical to main minus the tray: the systray
;; selection can only be owned by one bar, and two bars fighting for it is the
;; classic "my tray icons vanished" bug (i3-sugg §12.9).
inherit = bar/main
modules-right = pulseaudio backlight cpu memory temperature wlan battery powermenu
